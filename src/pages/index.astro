---
const posts = await Astro.glob('./blog/*.md');
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TechPulse Global</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
      body { font-family: 'Inter', sans-serif; }
    </style>
  </head>
  <body class="bg-gray-50 text-gray-900">
    <nav class="bg-white border-b py-6 sticky top-0 z-10 shadow-sm">
      <div class="max-w-4xl mx-auto px-6 font-black text-2xl tracking-tighter">
        TECHPULSE GLOBAL
      </div>
    </nav>

    <header class="py-20 bg-white border-b border-gray-100">
      <div class="max-w-4xl mx-auto px-6">
        <h1 class="text-6xl font-black mb-4 tracking-tight">Deciphering the <span class="text-blue-600 text-shadow-sm">Future</span>.</h1>
        <p class="text-xl text-gray-500 font-medium">Daily insights on the technology shaping our world.</p>
      </div>
    </header>

    <main class="max-w-4xl mx-auto px-6 py-16">
      <div class="grid gap-12">
        {posts.length === 0 ? (
          <div class="text-center py-20 border-2 border-dashed rounded-3xl text-gray-400">
            Waiting for AI to generate initial reports...
          </div>
        ) : (
          posts.reverse().map(post => (
            <article class="group">
              <time class="text-blue-600 font-bold text-sm tracking-widest uppercase">{post.frontmatter.date}</time>
              <h2 class="text-3xl font-bold mt-2 group-hover:text-blue-600 transition-colors">
                <a href={post.url}>{post.frontmatter.title}</a>
              </h2>
              <p class="text-gray-500 mt-4 leading-relaxed line-clamp-2">Latest technical analysis and executive summary from the TechPulse Editorial Team.</p>
              <div class="mt-6 flex items-center text-sm font-bold border-b-2 border-black w-max pb-1">
                READ ANALYSIS →
              </div>
            </article>
          ))
        )}
      </div>
    </main>

    <footer class="bg-white border-t py-20">
      <div class="max-w-4xl mx-auto px-6 text-sm text-gray-400">
        &copy; 2026 TechPulse Global Media Group. All rights reserved.
      </div>
    </footer>
  </body>
</html>
